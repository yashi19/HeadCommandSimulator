const FILE_ENCODING = "utf8";
const DEFAULT_LINES = 10;
const NO_FILE_ARGUMENT = "No File name was given";
const ILLEGAL_LINE_COUNT = "Illegal Line Count";
const ILLEGAL_BYTE_COUNT = "Illegal Byte Count";
const ILLEGAL_LINE_BYTE_ARG = "Can't combine line and byte counts";

module.exports = {
  FILE_ENCODING,
  DEFAULT_LINES,
  NO_FILE_ARGUMENT,
  ILLEGAL_LINE_COUNT,
  ILLEGAL_BYTE_COUNT,
  ILLEGAL_LINE_BYTE_ARG
};
